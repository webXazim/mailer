# CrescentSphere Mailer environment template
# Copy to .env for local Compose. Never commit real credentials.

# Application
APP_ENV=development
HTTP_ADDR=0.0.0.0:8080
CONSOLE_ORIGIN=http://localhost:8081
INTERNAL_API_URL=http://api:8080
RUST_LOG=info,sqlx=warn
FRONTEND_PORT=8081
VITE_API_URL=http://localhost:8080

# PostgreSQL and NATS
POSTGRES_USER=mailer
POSTGRES_PASSWORD=change-local-database-password
POSTGRES_DB=mailer
DATABASE_URL=postgres://mailer:change-local-database-password@postgres:5432/mailer
NATS_USER=mailer
NATS_PASSWORD=mailer-development
NATS_URL=nats://mailer:mailer-development@nats:4222
DB_MIN_CONNECTIONS=2
DB_MAX_CONNECTIONS=10
DB_ACQUIRE_TIMEOUT_SECONDS=5

# Application secrets
# Production: generate each with `openssl rand -base64 48`.
EVENT_INGEST_TOKEN=development-event-token-change-me
WEBHOOK_SIGNING_MASTER_KEY=development-webhook-master-key-change-me

# AWS SES and SQS
AWS_REGION=me-south-1
DOMAIN_PROVIDER=disabled
# On a VPS, use separate least-privilege IAM users for API and worker. On AWS compute, prefer IAM roles and leave these empty.
API_AWS_ACCESS_KEY_ID=
API_AWS_SECRET_ACCESS_KEY=
API_AWS_SESSION_TOKEN=
WORKER_AWS_ACCESS_KEY_ID=
WORKER_AWS_SECRET_ACCESS_KEY=
WORKER_AWS_SESSION_TOKEN=
# Required in production after creating the SNS topic and subscribed SQS queue.
SES_EVENTS_QUEUE_URL=
SES_EVENTS_TOPIC_ARN=

# Object storage (configured now; message blob persistence is added in a later phase)
OBJECT_STORAGE_PROVIDER=disabled
# Cloudflare R2: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_REGION=auto
OBJECT_STORAGE_BUCKET=
OBJECT_STORAGE_ACCESS_KEY_ID=
OBJECT_STORAGE_SECRET_ACCESS_KEY=
EMAIL_CONTENT_RETENTION_DAYS=30
API_KEY_RATE_LIMIT_PER_MINUTE=60
CLIENT_IP_RATE_LIMIT_PER_MINUTE=300
TRUST_PROXY_HEADERS=false
WORKSPACE_MONTHLY_EMAIL_LIMIT=100000
WORKSPACE_CONCURRENT_EMAIL_LIMIT=100
